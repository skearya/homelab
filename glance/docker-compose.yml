services:
  glance:
    image: glanceapp/glance
    container_name: glance
    restart: unless-stopped
    environment:
      - DOMAIN=${DOMAIN?}
      - GLUETUN_API_KEY=${GLUETUN_API_KEY?}
    env_file:
      - .env
    volumes:
      - ./config:/app/config
      - ./assets:/app/assets
